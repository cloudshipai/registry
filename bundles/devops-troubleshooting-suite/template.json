{
  "name": "devops-filesystem-tools",
  "description": "Filesystem tools for DevOps troubleshooting and analysis",
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y", 
        "@modelcontextprotocol/server-filesystem@latest",
        "{{ .PROJECT_ROOT }}"
      ]
    }
  }
}